-----------------------------------------------------------------------------------------------------------------------------------------
BASIC OF SQL WE LEARN
------------------------------------------------------------------------------------------------------------------------------------------
create-table-SQL

CREATE: 1st work create table
create table students
abc id INT primary key,                                         (--) ishko comment khte use one pr not run qureies)
abc name varchar (50) ,(--50 letter name may be 50 character) but not write these word  for remember only
salary deimal(10,2)
department varchar(50);



phir table create
somethig  data insert
   
   insert into studets (abvid, abc name,department,salary)
   values 
   (101,'rahul', 'sales', 250000),
   (102, 'ram', 'hr', 40000),

   
   select all and run

   
   data dekhne ke liye
   select * from tudents;

   show your data
   




-- now all things we learn will ahead --

ALTER: 
।DROP / TRUNCATE: 
Step 2: Data Manipulation Language (DML)
।INSERT: 
ा।UPDATE:                                                       (basic we learn from teacher gradually) all type
।DELETE:
   null
   not null
   in
   all 
   exixts
   not in
   on
   1nf 2nf 3nf
   schema
   distinct
   limit
   sum
   average
   highest and lowest
   order by 
   group by
   count
   unique
   where/ case
   count over
   having / where


   ---- subqueries function
   nested queries


   
 -----  join all types 
   inner 
   left
   right
   full
   
   aggregation 
Constraints NOT NULL:
UNIQUE:
PRIMARY KEY: 
।FOREIGN KEY: 
Basic Filtering
WHERE Clause: .AND, OR, NOT: BETWEEN & IN: 


   -- index
   unique index

   query planning


   -- transaction & acid
     -- 

 rollback
   commit

------
   stored procedures
  triggers
   delimiter
      ===
   producer and rajister new user



   trigger to log product deletation



   function to return full name

================================================
   
   window function ( heart of sql)
   row-number
   rank
   dense_rank
   lead/lag
   running total with sum(),, over()
   ======================================

   CTE and recurssion 
recursive operation
   
   Hierarchy traversal

   multiple cte




   ==========

   security and acces



   
